import { LightbooxDirective } from './lightboox.directive';

describe('LightbooxDirective', () => {
  it('should create an instance', () => {
    const directive = new LightbooxDirective();
    expect(directive).toBeTruthy();
  });
});
